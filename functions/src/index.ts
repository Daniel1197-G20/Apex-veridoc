export { registerOrganizationFn as registerOrganization, getMeFn as getMe } from "./modules/auth/auth.functions.js";

// TODO (next phases): export programmes, participants, attendance,
// credentials, verification (public HTTPS function), analytics, reports,
// and admin (/admin/* callables) here as each module is built.
