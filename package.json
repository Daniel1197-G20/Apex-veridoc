{
  "name": "apex-veridoc",
  "private": true,
  "scripts": {
    "client:dev": "npm --prefix client run dev",
    "client:build": "npm --prefix client run build",
    "functions:build": "npm --prefix functions run build",
    "emulators": "npm run functions:build && npx firebase-tools emulators:start --project demo-apex-veridoc",
    "dev": "npm run client:dev",
    "install:all": "npm install --prefix client && npm install --prefix functions"
  },
  "devDependencies": {
    "firebase-tools": "^15.27.0"
  },
  "dependencies": {
    "firebase": "^12.17.1"
  }
}
